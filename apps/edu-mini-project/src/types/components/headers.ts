/* This code snippet is defining an interface named `HeaderProps` in TypeScript. The interface has a
single property `title` of type `string`. By using the `export` keyword, this interface can be
accessed and used in other files or modules. */
export interface HeaderProps {
	title: string;
}
